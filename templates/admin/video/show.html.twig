{% extends 'base.html.twig' %}

{% block title %}Video{% endblock %}

{% block body %}
<section class="container section-dashboard-admin">
    <h1>Video</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ video.id }}</td>
            </tr>
            <tr>
                <th>MediaLink</th>
                <td>{{ video.mediaLink }}</td>
            </tr>
            <tr>
                <th>Embed</th>
                <td>{{ video.embed }}</td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('app_admin_video_edit', {'id': video.id}) }}" class="btn btn-primary my-2">Modifier</a>

    {{ include('admin/video/_delete_form.html.twig') }}
    <a href="{{ path('app_admin_video_index') }}">Retour à la liste</a>
</section>
{% endblock %}

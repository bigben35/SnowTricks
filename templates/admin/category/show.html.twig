{% extends 'base.html.twig' %}

{% block title %}SnowTricks - Catégorie
{% endblock %}

{% block body %}
	<section class="container section-dashboard-admin">
		<h1>Catégorie</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Nom</th>
					<td>{{ category.name }}</td>
				</tr>
			</tbody>
		</table>


		<a href="{{ path('app_admin_category_edit', {'id': category.id}) }}">Modifier</a>
		{{ include('admin/category/_delete_form.html.twig') }}
		<a href="{{ path('app_admin_category_index') }}">Retour à la liste</a>

	</section>
{% endblock %}
